# electron 43 的依赖链(@electron/get 5 为 ESM-only)要求 Node >= 22.12.0。
# 缺少该版本时 electron 的 postinstall 会以 ERR_REQUIRE_ESM 失败并留下残缺 node_modules，
# 默认的 EBADENGINE 只是警告、不会阻止安装 —— 因此在这里把它升格为安装期硬失败。
engine-strict=true
